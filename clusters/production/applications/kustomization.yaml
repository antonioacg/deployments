apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - apps/
  - pvc/

labels:
- includeSelectors: true
  includeTemplates: true
  pairs:
    tier: applications
    managed-by: flux