apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - vault-minio/
  - vault/
  - external-secrets/

labels:
- includeSelectors: true
  includeTemplates: true
  pairs:
    tier: bootstrap
    managed-by: flux