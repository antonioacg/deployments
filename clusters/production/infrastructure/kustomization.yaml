---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  name: infrastructure
  namespace: flux-system

resources:
  - ingress-nginx/

labels:
  - includeSelectors: true
    pairs:
      tier: infrastructure
      managed-by: flux

commonAnnotations:
  note: "Infrastructure components deployed via GitOps"